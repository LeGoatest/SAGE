# canon/rules/security/scope_required.yaml
id: SECURITY_SCOPE_REQUIRED
layer: security
effect: require
match:
  actions: [WRITE_FILE, PATCH_FILE, DELETE_FILE]
requires: ["DECLARED_SCOPE"]
reason_code: SCOPE_REQUIRED
message: "Mutations require declared scope."
